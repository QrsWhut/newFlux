<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chat.dal.dao.TurnToolCallMapper">

    <insert id="insert" parameterType="com.example.chat.dal.model.TurnToolCallDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO turn_tool_call (
            turn_id, agent_step_no, call_no, call_id, tool_name,
            arguments_content, status, result_summary, result_reference,
            error_code, error_message, latency_millis, start_time, finish_time
        ) VALUES (
            #{turnId}, #{agentStepNo}, #{callNo}, #{callId}, #{toolName},
            #{argumentsContent}, #{status}, #{resultSummary}, #{resultReference},
            #{errorCode}, #{errorMessage}, #{latencyMillis}, #{startTime}, #{finishTime}
        )
    </insert>

    <select id="selectByTurnIds" resultType="com.example.chat.dal.model.TurnToolCallDO">
        SELECT id, turn_id, agent_step_no, call_no, call_id, tool_name,
               arguments_content, status, result_summary, result_reference,
               error_code, error_message, latency_millis, start_time, finish_time,
               create_time, update_time
        FROM turn_tool_call
        WHERE turn_id IN
        <foreach collection="turnIds" item="turnId" open="(" separator="," close=")">
            #{turnId}
        </foreach>
        ORDER BY turn_id ASC, agent_step_no ASC, call_no ASC
    </select>
</mapper>
